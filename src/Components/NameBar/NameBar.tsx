import { IconButton, Typography } from '@mui/material';
import './namebar.css';

export default function NameBar(){
    

    return (
        <div className="name-bar">
            
            <Typography variant='h5' m='5px'>NAME BAR</Typography>
        </div>
    );
}